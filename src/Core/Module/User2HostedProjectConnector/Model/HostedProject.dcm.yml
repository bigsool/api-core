HostedProject:
    indexes:
          creator:
              columns:
                  - creator
    manyToOne:
      creator:
          targetEntity: Core\Model\User
          inversedBy: hostedProjects
          joinColumns:
              creator:
                  referencedColumnName: id
                  nullable: false
          cascade: ["persist"]
SystemMessage:
  manyToOne:
        user:
            targetEntity: Core\Model\User
            inversedBy: systemMessages
            joinColumns:
                user_id:
                    referencedColumnName: id
                    nullable: false
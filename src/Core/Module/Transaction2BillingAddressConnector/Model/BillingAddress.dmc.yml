BillingAddress:
    id:
        transaction:
              associationKey: true
    oneToOne:
        transaction:
            targetEntity: Core\Model\Transaction
            inversedBy: billingAddress
            joinColumns:
                transaction_id:
                    referencedColumnName: id
                    nullable: false
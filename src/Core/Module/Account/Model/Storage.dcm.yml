Storage:
    oneToOne:
        company:
            targetEntity: Core\Model\Company
            inversedBy: storage
            cascade: ["persist"]
            joinColumns:
                company_id:
                    referencedColumnName: id
                    nullable: true
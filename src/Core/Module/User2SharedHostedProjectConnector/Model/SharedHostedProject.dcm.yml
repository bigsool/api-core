SharedProject:
     manyToOne:
            participant:
                targetEntity: Core\Model\User
                inversedBy: sharedHostedProjects
                joinColumns:
                    participantId:
                        referencedColumnName: id
                        nullable: false
                cascade: ["persist"]
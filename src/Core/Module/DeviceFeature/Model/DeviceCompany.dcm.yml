Core\Model\DeviceCompany:
    type: entity
    table: deviceCompany
    indexes:
        company_id:
            columns:
                - company_id
        IDX_AA21A64B94A4C7D4:
            columns:
                - device_id
    uniqueConstraints:
        device_id:
            columns:
                - device_id
                - company_id
    id:
        id:
            type: integer
            nullable: false
            unsigned: false
            comment: ''
            id: true
            generator:
                strategy: IDENTITY
    manyToOne:
        device:
            targetEntity: Core\Model\Device
            inversedBy: deviceCompanies
            joinColumns:
                device_id:
                    referencedColumnName: id
                    nullable: false
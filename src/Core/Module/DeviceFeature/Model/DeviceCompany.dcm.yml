DeviceCompany:
    type: entity
    table: deviceCompany
    indexes:
        IDX_AA21A64B94A4C7D4:
            columns:
                - device_id
    uniqueConstraints:
        device_id:
            columns:
                - device_id
                - company_id
    id:
        id:
            type: integer
            nullable: false
            unsigned: false
            comment: ''
            id: true
            generator:
                strategy: IDENTITY
    manyToOne:
        device:
            targetEntity: Core\Model\Device
            inversedBy: deviceCompanies
            joinColumns:
                device_id:
                    referencedColumnName: id
                    nullable: false
        company:
            targetEntity: Core\Model\Company
            inversedBy: deviceCompanies
            joinColumns:
                company_id:
                    referencedColumnName: id
                    nullable: false
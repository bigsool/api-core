ProjectRole:
      manyToOne:
          user:
              targetEntity: Core\Model\User
              inversedBy: userProjectRoles
              joinColumns:
                  user_id:
                      referencedColumnName: id
                      nullable: false
ProjectPersons:
    manyToOne:
            creator:
                targetEntity: Core\Model\User
                inversedBy: projectTypicalDescriptions
                joinColumns:
                    creator:
                        referencedColumnName: id
                        nullable: false
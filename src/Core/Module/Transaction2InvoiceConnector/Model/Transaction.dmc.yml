Transaction:
    indexes:
       invoice_id:
           columns:
               - invoice_id
    oneToOne:
            invoice:
                targetEntity: Core\Model\Invoice
                inversedBy: transaction
                joinColumns:
                    invoice_id:
                        referencedColumnName: id
Contact:
    oneToOne:
        abcompanyContact:
           targetEntity: Core\Model\ABCompanyContact
           mappedBy: contact
           cascade: ["persist"]
        abpersonContact:
           targetEntity: Core\Model\ABPersonContact
           mappedBy: contact
           cascade: ["persist"]